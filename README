Программа для преобразования URL с элементами вида %XX в читаемый текст. Работает в окружении с кодирвокой UTF-8. Берет URL из стандартного потока и отдает результат туда же.

Скачивание:
  git clone 

Сборка:
  cd urltotxt
  make

Запуск:
  ./urltotxt

